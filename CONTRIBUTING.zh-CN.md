# 贡献指南

[English](CONTRIBUTING.md)

每个 Hook 都必须保持最小化，在必要时锁定版本，并只覆盖文档指定的软件包/进程。

1. 说明精确的设备、ROM、Android、LSPosed、模块与目标应用版本，只使用公开构建标识。
2. 说明平台或应用为何失败、Hook 了哪个方法，以及更小作用域为何不足。
3. 任何 `system` Hook、作用域扩展、签名/版本守卫移除、token 处理或持久化改动均视为安全敏感。
4. 不得提交专有 APK/DEX/框架代码、反编译第三方源码、签名密钥、含私钥证书、注册 ID、token、账户数据、消息正文、私有数据库、序列号或原始日志。
5. 编译 stub 必须保持仅编译用途，不得把 Xposed API 类打包进 APK。
6. 完成 PowerShell 解析、微信 token bridge 安全测试、Java 编译及相关 Apktool 重建/安装测试。
7. 记录测试 APK 的 SHA-256 和签名证书指纹，但绝不公开私钥。

兼容性 Issue 应包含脱敏激活证据、精确作用域、预期/实际行为及禁用或卸载后的完整回滚结果。
